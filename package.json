{
  "scripts": {
    "build": "node scripts/build-static.js",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "xlsx": "^0.18.5",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "wrangler": "^4.95.0"
  }
}
